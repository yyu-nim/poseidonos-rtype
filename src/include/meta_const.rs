pub const STRIPE_CHUNKS_SIZE: u64 = 256;
pub const CHUNK_SIZE: u64 = STRIPE_CHUNKS_SIZE * 1024;
pub const SEGMENT_SIZE: u64 = STRIPE_CHUNKS_SIZE * 1024 * 1024; /* 256 MB */
pub const STRIPES_PER_SEGMENT: u32 = (SEGMENT_SIZE / CHUNK_SIZE) as u32;