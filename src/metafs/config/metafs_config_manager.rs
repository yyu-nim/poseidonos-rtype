use log::info;

pub struct MetaFsConfigManager;

impl MetaFsConfigManager {

    pub fn new() -> MetaFsConfigManager {
        MetaFsConfigManager
    }

    pub fn Init(&self) {
        // TODO
        info!("need to build a config");
    }

}